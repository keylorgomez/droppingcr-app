/**
 * Active locale accessor.
 *
 * Usage:  import { t } from '../lib/i18n'
 *         t.toast.profileUpdated
 *         t.actions.save
 *
 * To switch language: change the import below to the desired locale file.
 */
import { es } from "../locales/es";

export const t = es;
